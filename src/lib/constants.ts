/**
 * Constantes globales de la aplicación.
 */

/**
 * Clave usada por el middleware `persist` de Zustand para guardar las
 * preferencias de UI (modo oscuro y widgets visibles) en `localStorage`.
 * Debe mantenerse estable, ya que es el contrato de persistencia del cliente.
 */
export const UI_STORE_STORAGE_KEY = "chile-en-datos-ui";